import { screen, within } from '@testing-library/react';
import {Songs}  from './index'


describe('Songs', ()=> {
    beforeEach(() => {
        // const songs = { rank: 1, song: 'Gold Digger' };
        // render(<Songs song={songs}/>);
    });

    test('it displays props.topSongs', () => {
        // const songTitle = screen.getByRole('header');
        //     expect(songTitle).toBeInTheDocument()
    })


})
